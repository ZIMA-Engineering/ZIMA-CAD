# Windows build 2026092401

## Changes

- Modeling reference tables follow the shared reference-entry presentation,
  including numbered rows, independent input and inspection states, predictable
  resizing and command-specific removal behavior.
- The 3D Curve editor supports single-click point editing, explicit row selection
  for reordering, direction controls and correctly sized numeric fields on Windows.
  Curve points remain visible for attachment; editing highlights only the target.
- Whole-Origin reference entry publishes the scene once per completed operation.
  The measured imported-Part case improved from 3335 ms to 964 ms on the development
  machine; this is not a cross-machine performance guarantee.
- Sweeps expose persistent attachment endpoints and highlight their own axes.
- GUI text uses the system font by default, with the bundled font as fallback.
  Sketcher, View and Drawing technical text retain the ISO font.
- Hover is green and confirmed selection, inspection and previews are azure.
  Sketch buttons are white, confirmation checkmarks turn black on hover, and
  menus share consistent icons. Tree ordering remains available by drag/drop;
  redundant Move Up/Down context actions were removed.
- Sketch midpoint acquisition, selected-entity colouring, vertical dimension
  clearance and passive Assembly sketch display were corrected.
- The Applications menu has mode icons. The Window menu remains available for
  selecting documents when their tabs do not all fit.

The source also includes the already committed editable Sheet Transition and
Sheet from Body/DXF corrections since build 2026092301. See the feature documents
for their manufacturing limits and supported geometry.

## Verification scope

All five UI languages remain supported. This release does not add document-format
fields or change native filename extensions. The development launcher remains
`zima-cad.bat`; portable installations keep their own launcher and user data.

All 24 selected native/GUI regressions passed, including Curve/Sweep, reference
entry, three Windows numeric-control styles, font selection, edge treatment,
midpoint acquisition, Sheet Transition and Sheet exchange. The focused nested
Assembly Sketch-return check passed separately. Packaging tests passed (9 cases,
including 2 platform-specific skips), as did all 16 publisher-input tests.
Logs are `build/release-2401-regressions.log`, `release-2401-sketch-return.log`,
`release-2401-package-tests.log` and `release-2401-publisher-tests.log`.

Linux is not part of this build.

Known limitations from the development verification: the broader Section GUI
scenario expects a Drawing sheet/view Rename action that is not implemented in
that menu branch. The older nested-Body GUI scenario fails its active/passive
later-history assertion before the Sketch return operation. The focused return
scenario passes independently; these broader checks are not claimed as passing.

## Portable Windows acceptance

The candidate was built from exact commit
`d0b259e0c7205b88c12618e3713037398a0d409f`. Native dependency closure, archive path
and collision checks, CRC/SHA-256 and clean-environment GUI/CLI smoke passed.
Smoke includes a calculated 6000 mm3 Part, save/reopen and PDF/JPEG export.
The signed archive passed the same smoke checks and a fresh extraction reported
`trusted: true` using the production updater.

- Archive: `ZIMA-CAD-2026092401.zip` (78,846,045 bytes).
- Signed SHA-256: `4345b55380bd4c7e97267b8d1291232a14810992f6140d6ee4f8a531894b4628`.
- Candidate SHA-256: `9e6c47b419d69dca4b7d48df016f5c83e5324f5fa51279408fa4cd356ae0970c`.
- Evidence: `.dist-output/release-2026092401/`,
  `build/release-2401-candidate.log`, `release-2401-trust.log` and
  `release-2401-lifecycle.log`.

Packaged Save All, Discard, Cancel, failed save, pending edit, five-language
restart and saved-document reopening passed. This older lifecycle fixture checks
literal lowercase names; its initial run failed the warning-text assertion under
uppercase naming. The successful run set `DocumentNames/Uppercase=false` only in
the disposable extracted installation's user configuration. The signed archive,
factory defaults and initial default-configuration smoke were unchanged.

## Publication

Published with user authorization on 2026-09-24 at 11:10:45 UTC as the latest
stable [Windows release 2026092401](https://github.com/ZIMA-Engineering/ZIMA-CAD/releases/tag/ZIMA-CAD-2026092401).
The tag identifies the exact source commit recorded above. All three assets
(archive, manifest and signature) were downloaded again and matched their local
SHA-256 hashes. The signed production 2026092301 updater offered 2026092401 as
available and installable; this check did not install or activate an update.
Publication evidence is in `build/release-2401-public.json`,
`release-2401-publish.log` and `release-2401-public-check.log`.
