# ZIMA-CAD 2026091508

- Family Table instances share one native parent file, common model edits and
  Undo/Redo. Table-controlled changes update the edited row; other changes update
  the shared model and variants.
- Save from an instance saves its family. Save As creates an independent native
  copy. Instances cannot own nested families.
- Stable row identities keep Drawing references valid after instance renaming.
  An empty Drawing can select an open Part or Assembly variant before Insert View.
- Drawing Text adds multiline sheet annotations below Dimension, with shared
  internal properties, selection, purple anchor, dragging, editing and Undo.
  Native files and PDF/DXF/image exports retain the text.
- Includes the preceding solid-aware linear/circular Pattern and Family Table
  reference-picker work from development builds 2026091506 and 2026091507.
- Native formats: Part INI 25 / payload 49, Assembly payload 33 and Drawing
  INI 16 / payload 8. Start templates and native fixtures are updated.

See [Family Table](../FAMILY_TABLE.md) and [Drawing text](../DRAWING_TEXT.md).
Windows development builds passed, including all test targets. The 29 selected
native/GUI contracts passed after correcting source-identity error propagation
and the catalog count for the two previously added Family commands. The Family
GUI also inserts a view from an Assembly instance. Fourteen packaging/publisher
input tests passed. PDF text extraction independently confirmed all three lines.
Follow-up tests verify consecutive instance edits retain family routing and
monotonic revisions, and updated variant geometry reaches Assembly occurrences
without Assembly regeneration. The final Family GUI contract passed again.
The repository-root `zima-cad.bat` remains the development entry point.

## Signed Windows acceptance

Published on 2026-09-15 at 18:00:05 UTC as the latest stable
[GitHub release](https://github.com/ZIMA-Engineering/ZIMA-CAD/releases/tag/ZIMA-CAD-2026091508).
Tag `ZIMA-CAD-2026091508` identifies source commit
`05fccf205fd434dbeb6860db9d9c0596a580ebaf`. A clean detached checkout supplied the
candidate builder and signing finalizer; user settings and working files were
excluded. Candidate and final signed ZIPs passed dependency, path, collision,
CRC, SHA-256 and native Windows smoke checks, including GUI/CLI startup, a saved
and reopened 6000 mm3 Part, PDF/JPEG export and shared configuration preservation.

The published assets are `ZIMA-CAD-2026091508.zip` (70,391,897 bytes),
`update-manifest.json` and `update-manifest.sig`. Their remote names, sizes and
SHA-256 digests match the accepted local files under
`.dist-output/release-2026091508/`. Signed ZIP SHA-256:

```text
16e6c5976c505e8babbb7349123d9bb455b4eb838391c93913582d9a137a966b
```

A fresh extraction passed production bootstrap verification (`trusted: true`).
The production updater in a disposable signed `2026091505` installation verified
the public manifest and reported `available`, `installable: true`, for `2026091508`.
The new version reported `current`. This check verified discovery, not activation
or rollback of an installed update. Linux runtime acceptance remains on Linux.

Evidence: `build/release-08-final-candidate.log`, `build/release-08-signed.log`,
`build/release-08-bootstrap.log`, `build/release-08-upload-inventory.json`,
`build/release-08-public.json`, `build/release-08-public-check.log`, and
`build/release-08-current-check.log`. The local signed `.validation.json` records
the exact source commit, archive hash and passed Windows smoke gate.
