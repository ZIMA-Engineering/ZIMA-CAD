# ZIMA-CAD 2026091902

Windows follow-up release with corrected Twisted Sheet material-side attachment
and creation previews, plus explicit exclusive All/individual choices in Unbend
and Bend Back. Profile-chain unfold/fold-back and Sheet Cut with rotated Origins
have additional regression coverage.

Part format is INI 41 / JSON 65. The updated start Part is included. Backward
compatibility with earlier native Part formats is not supported; preserve
original documents before opening them with another release.

Fifteen focused native/GUI contracts passed during implementation. Signed
Windows package acceptance is recorded after candidate and publication checks.
This release contains Windows only; Linux was not rebuilt.

## Signed Windows acceptance

- Source/tag commit: `52ab76c4cbfc4e9d6e7930cedcf56564d88d1881`.
- ZIP: `ZIMA-CAD-2026091902.zip`, 71,853,875 bytes.
- SHA-256: `3dc15f892b56bfedc4610e1b6382df981719f3a04490ee16b28c36d4008f4b21`.
- A fresh clean compilation passed after the reused-build candidate was rejected
  for stale executable identity. The rejected output was never published.
- Candidate and signed archive passed dependency, path/collision, CRC, SHA-256,
  source identity, GUI startup, 6000 mm3 Part save/reopen and PDF/JPEG checks.
- Seven Windows packaging checks and all 16 publisher tests passed; two
  Linux-only package checks were skipped on Windows.
- Fresh signed extraction reports `trusted:true`, the exact source commit and
  `source_modified:false` with developer paths removed.

The [GitHub release](https://github.com/ZIMA-Engineering/ZIMA-CAD/releases/tag/ZIMA-CAD-2026091902)
is published. All three asset sizes and SHA-256 digests match local acceptance;
public downloads were checked. Production update discovery from 2026091901
reports `available` and `installable:true`, without installing the update.
The published assets remain unchanged. GitHub reports its repository-level
`immutable` flag as false; this does not change the project's no-replacement policy.

Logs are `build/release-1902-*`. Verification is focused on this follow-up and
packaging; the known title-sketch regression and first-open file-list delay from
2026091901 are not claimed as resolved. No fresh Windows OS or Linux run was made.
