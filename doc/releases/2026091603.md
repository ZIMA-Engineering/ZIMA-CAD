# ZIMA-CAD 2026091603

- Sheet Metal now offers Selection and Sheet Metal Properties in the right-hand
  command panel. The properties shortcut opens File Settings > Sheet Metal.
- Optional default material thickness and the material K factor are stored with
  the Part, support Undo/Redo and can also be edited through `document.settings`.
  Existing geometry is preserved when changing these defaults.
- The Part application dropdown switches Modeling/Sheet Metal and remembers the
  choice per open document. The contextual Insert menu shares toolbar actions.
- Extrusion/Revolution profile status reflects actual Sketch connectivity.
  Saving clears the dirty marker without resizing document tabs.
- Includes Surface Extrusion/Revolution, yellow surface display and visibility
  control, Drawing surface exclusion, and the Sketcher analysis-row Undo fix.
- Includes family variant insertion/replacement in Assemblies and Drawings,
  historical Body measurement with centroid inspection, and Drawing BOM balloons
  from the preceding development commits since release 2026091508.

The first Bend/Unbend command is specified but not implemented in this release.
See [Sheet Metal](../SHEET_METAL.md), [Application tools](../APPLICATION_TOOLS.md)
and [Surface profiles](../SURFACE_PROFILES.md).

## Validation

Seven targeted native/GUI suites passed in 130.26 seconds. They cover metadata
transactions and persistence, invalid input, geometry preservation, Updates UI,
full workspace startup, the Sheet Metal shortcut and View confirmation,
application switching, stable tab widths, Surface profiles and Sketch Offset.
Fourteen packaging and publisher input-gate tests passed. GUI screenshots were
inspected. Development remains available through repository-root `zima-cad.bat`.

## Signed Windows acceptance

Published on 2026-09-15 at 23:31:13 UTC (2026-09-16 in Prague) as the latest stable
[GitHub release](https://github.com/ZIMA-Engineering/ZIMA-CAD/releases/tag/ZIMA-CAD-2026091603).
Tag `ZIMA-CAD-2026091603` identifies source commit
`20af33d8e6d2e5971378c04c806cf9d81fec95ff`. A clean detached checkout supplied the
candidate builder and signing finalizer. User settings and unrelated working
files were excluded; the reused build stage first verified its previous source
against its recorded Git commit.

Candidate and finalized signed archives passed dependency, path, collision, CRC,
SHA-256 and native Windows smoke checks. These cover launcher/GUI/CLI identity,
a saved and reopened 6000 mm3 Part, PDF/JPEG export, rendered GUI startup and
preservation of shared user configuration.

The published assets are `ZIMA-CAD-2026091603.zip` (70,767,027 bytes),
`update-manifest.json` and `update-manifest.sig`. Their remote names, sizes and
SHA-256 digests match the accepted local files under
`.dist-output/release-2026091603/`. Signed ZIP SHA-256:

```text
4d0c890627ee0fe7e8072a940b0a820c23197e1fd23eb9259697c89da580eac9
```

A fresh extraction passed production bootstrap verification (`trusted: true`).
The production updater in a disposable signed `2026091508` installation verified
the public manifest and reported `available`, `installable: true`, for `2026091603`.
The new version reported `current`. This verifies public update discovery;
activation and rollback of this particular release were not exercised.
Linux runtime acceptance remains on the Linux host.

Evidence: `build/release-1603-candidate.log`, `build/release-1603-signed.log`,
`build/release-1603-bootstrap.log`, `build/release-1603-upload-inventory.json`,
`build/release-1603-public.json`, `build/release-1603-public-check.log` and
`build/release-1603-current-check.log`. The local signed `.validation.json`
records the exact source commit, archive hash and passed Windows smoke gate.
