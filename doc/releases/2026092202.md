# ZIMA-CAD 2026092202 — Windows

## Changes

- Assembly Drawing projection uses a spatial triangle index while preserving
  exact visibility clipping and curve refinement. The reported five-component
  assembly now projects isometric edges in about 0.6 seconds, compared with an
  original run stopped after more than a minute. Complete source projection,
  including annotations, measured about 4.7 seconds locally.
- Skeleton occurrences are excluded from Assembly drawings, including nested
  occurrences and their reference geometry. Assembly display is unchanged.
- New Skeleton Parts use `start_skeleton.prtz`: an empty active Body and black
  surfaces with approximately 70% transparency. Classification remains based
  exclusively on the `*_skeleton.prtz` filename.
- Running dimensions are independent entities with their own tolerances. They
  can continue from an existing dimension, move through their common datum,
  and convert to linear dimensions. Reference entry follows shared progressive
  rows, and the dimension properties dialog has a compact height.
- Free parallel dimensions support alignment; witness lines support editable
  jogs and breaks with draggable grips. Commands have localized labels/icons.
- Drawing point/curve hover, curved edge visibility, saved thread projection,
  model dimension units, stacked tolerance size and Tree icons are corrected.
- Title-block text supports Today's date and configurable lists. Projection
  symbols use shared center geometry and centerlines. All five title blocks
  include central mass/quantity unit labels. The values dialog is narrower.

This release also includes the Drawing features documented in
[2026092201](2026092201.md), whose signed archive was prepared locally but not
published. Existing drawings embed their title blocks; reinsert a library title
block to adopt changed labels and projection geometry.

## Verification

Focused projection, source filtering, Drawing UI, thread, native document and
translation tests passed. Skeleton creation and enabled modeling commands were
verified in all five UI languages. Regression tests cover open/saved sources,
nested Skeletons, unchanged Assembly display and exact partial occlusion.
The projection optimization and Skeleton template do not change modeling
precision or document formats.

This document does not claim a complete repository-wide test pass or Linux
verification.

## Signed Windows acceptance

Source commit: `484e78540ffa1a81f9401fe9bcb55dd06e0400d7`, tagged
`ZIMA-CAD-2026092202`. A fresh staging build used only committed Git source.
The candidate and signed archive passed dependency deployment, archive safety,
CRC, SHA-256 and native Windows smoke checks (GUI/CLI startup, 6000 mm3 Part,
save/reopen and PDF/JPEG export). Seven applicable packaging tests and sixteen
publisher gate tests passed; two Linux-only packaging checks were skipped.

A fresh signed extraction passed production bootstrap trust (`trusted: true`),
clean build identity (`source_modified: false`) and creation of Parts, active
Skeleton Bodies, Assemblies and localized Drawings in all five UI languages,
with developer SDK paths removed from PATH.

Archive: `ZIMA-CAD-2026092202.zip`, 77,566,778 bytes. SHA-256:

```text
0a70ad2f48098b896c111f7c8ca0626cc0c8f982036555f9cb338fd01751a5ed
```

Local signed assets: `.dist-output/release-2026092202/`. Evidence:
`build/release-2202-candidate.log`, `build/release-2202-signed.log`,
`build/release-2202-package-check.log`, `build/release-2202-new-document.log`,
and the archive validation report. The development launcher remains
repository-root `zima-cad.bat`.

Published on GitHub at 2026-09-22 10:09:51 UTC as the latest stable
[Windows release](https://github.com/ZIMA-Engineering/ZIMA-CAD/releases/tag/ZIMA-CAD-2026092202).
All three uploaded assets were downloaded again and their SHA-256 hashes
matched the accepted local files. The production updater from Windows
2026092102 verified the public signed manifest and reported 2026092202 as
available and installable. Evidence: `build/release-2202-remote-hashes.json`,
`build/release-2202-public.json` and `build/release-2202-public-check.log`.

## Follow-up reported during packaging

The user confirmed that inserting Assembly views works after the performance
fix. They also reported possible Origin-like lines and a thread lead-in that
remains visible in `ze0001-0000-0000.drwz`. Those two reports are not yet
diagnosed or fixed; the user explicitly requested completing this release
before addressing them.
