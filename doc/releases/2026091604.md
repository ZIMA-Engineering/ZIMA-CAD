# ZIMA-CAD 2026091604

- Sketcher distances now display positive magnitudes consistently, including
  dimensions from the origin and the built-in axes. Positive input preserves
  the current side; negative input reverses it and displays a positive value
  again. The solver retains signed coordinates internally. Existing constraints
  must permit the change; rejected edits leave the Sketch unchanged.
- Dimension value fields accept arithmetic with `+`, `-`, `*`, `/`, parentheses
  and decimal commas. Invalid expressions remain available for correction.
- Selecting a dimension immediately after creating it no longer loses the
  selection during command cleanup, allowing normal double-click editing.
- Sketcher Coincident accepts both point-then-axis and axis-then-point selection.
- Sheet Metal includes the first sketch-based Bend/Unbend command, document
  thickness and K-factor defaults, and per-feature overrides. Default thickness
  is 1 mm and no longer requires a document-level enable checkbox.
- Drawing Text offers immediate cursor placement, an empty-text dash, multiline
  input and a symbol picker. Default text is green and 2.5 mm high. Dimension
  text clearance preserves the dimension line beneath the value.

See [Sketcher](../SKETCHER.md), [Sheet Metal](../SHEET_METAL.md), and
[Drawing Text](../DRAWING_TEXT.md). Development continues to start through the
repository-root `zima-cad.bat`.

## Validation

Nine targeted suites passed: Sketcher solver, dimension CLI, Family Table,
shared properties UI, Updates UI, application tools, dimension entry UI,
Coincident UI and Bend commands. The origin/axis regression checks both sides
of X/Y, both origin pick orders, repeated negative edits, magnitude limits,
fixed-point rejection, reference values, Undo/Redo and native save/reload.
Properties tests cover ordinary point pairs, origin references and both forms
of axis distance. Fourteen archive/source and publisher input-gate tests passed.

Local evidence: `build/sketch-all-positive-core-tests.log`,
`build/sketch-all-positive-gui-tests.log`, `build/release-1604-package-tests.log`
and `build/release-1604-publisher-tests.log`.

## Signed Windows acceptance

Published on 2026-09-16 at 07:26:54 UTC as the latest stable
[GitHub release](https://github.com/ZIMA-Engineering/ZIMA-CAD/releases/tag/ZIMA-CAD-2026091604).
Tag `ZIMA-CAD-2026091604` identifies source commit
`3d89418b3253924de2102ea24c6e9312e397056c`. A clean detached checkout supplied the
candidate builder and signing finalizer. The reused staging tree verified its
previous source against its recorded commit before exporting the new source.
Uncommitted user configuration and unrelated files were excluded.

The candidate and finalized signed archives passed dependency, path, collision,
CRC, SHA-256 and native Windows smoke gates. These cover launcher/GUI/CLI
identities, a saved and reopened 6000 mm3 Part, PDF/JPEG export, rendered GUI
startup and preservation of shared configuration. The candidate View capture
was visually inspected. A fresh signed extraction passed production bootstrap
verification (`trusted: true`).

Published assets: `ZIMA-CAD-2026091604.zip` (70,863,875 bytes),
`update-manifest.json` and `update-manifest.sig`. Remote names, sizes and SHA-256
digests matched local acceptance before and after publication. Signed ZIP SHA-256:

```text
08ea279073e9223691a2f7bdf8c24d14bbd5e3c9d6c6361f0bab6cfd49307033
```

The production updater in a disposable signed `2026091603` installation verified
the public manifest and reported `available`, `installable: true`, for
`2026091604`. The new version reported `current`. This verifies public discovery;
activation and rollback of this release were not exercised. Linux runtime
acceptance remains on the Linux host.

Evidence: `build/release-1604-candidate.log`, `build/release-1604-signed.log`,
`build/release-1604-bootstrap.log`, `build/release-1604-upload-inventory.json`,
`build/release-1604-public.json`, `build/release-1604-public-check.log` and
`build/release-1604-current-check.log`. The local signed `.validation.json`
records the exact source commit, archive hash and passed Windows smoke gate.
